# MITRE ATT&CK Mitigations Lookup Tool

Утилита для поиска всех техник и под-техник MITRE ATT&CK, которые смягчаются заданной контрмерой (mitigation). Поддерживает несколько форматов вывода и полностью соответствует принципам Cloud Native.

## 🚀 Возможности

- **Поиск по идентификатору контрмеры** (например, `M1037`) или по её названию
- **Автоматическое скачивание** актуальной STIX-базы ATT&CK Enterprise
- **Гибкое кэширование** с поддержкой Docker volumes, tmpfs и отключения кэша
- **Несколько форматов вывода**:
  - Таблица (по умолчанию)
  - JSON
  - CSV
  - nGQL-запросы для Nebula Graph
- **Cloud Native готовность** - 12-Factor App, stateless, Docker-ready
- **Безопасность** - непривилегированный пользователь, read-only режим

## 📦 Установка и сборка

### Локальная сборка
```bash
go build -o mitremit mitre-mitigates.go
```

### Docker сборка
```bash
make docker-build
```

### Сборка для всех платформ
```bash
make build-all
```

## 🛠️ Использование

### Базовый пример (таблица):
```bash
./mitremit -mitigation M1037
```

### JSON вывод:
```bash
./mitremit -mitigation M1037 -json > output.json
```

### Поиск по названию:
```bash
./mitremit -mitigation-name "Filter Network Traffic" -csv
```

### Генерация nGQL-запросов:
```bash
./mitremit -mitigation M1037 -ngql > nebula_inserts.ngql
```

### Отключение кэша (для CI/CD):
```bash
./mitremit -mitigation M1037 --no-cache
```

## 🐳 Docker использование

### Сборка образа:
```bash
make docker-build
```

### Запуск с локальным кэшем:
```bash
make docker-run
```

### Запуск без кэша:
```bash
make docker-run-nocache
```

### Запуск с Docker volume:
```bash
make docker-run-volume
```

### Shell в контейнере для отладки:
```bash
make docker-shell
```

### Полное тестирование Docker образа:
```bash
make docker-test
```

## ⚙️ Параметры командной строки

### Основные параметры:
- `-mitigation` — идентификатор контрмеры ATT&CK (например, `M1037`)
- `-mitigation-name` — полное название контрмеры (регистронезависимый поиск)
- `-json` — вывод в формате JSON
- `-csv` — вывод в формате CSV
- `-ngql` — вывод INSERT-запросов для Nebula Graph
- `-debug` — дополнительная отладочная информация
- `-h` — справка

### Управление кэшем:
- `--cache-dir DIR` — указать директорию для кэша
- `--no-cache` — отключить кэширование
- `--force-refresh` — принудительно обновить кэш

### Переменные окружения:
- `MITRE_CACHE_DIR` — директория для кэша (имеет приоритет над флагами)

## 📊 Пример вывода

### Таблица (по умолчанию):
```
MITIGATION       Filter Network Traffic (M1037)
----------------------------------------------------------------
TECHNIQUE ID     TECHNIQUE NAME
T1071            Application Layer Protocol
T1565            Data Manipulation
T1573            Encrypted Channel
```

### JSON:
```json
[
  {
    "external_id": "T1071",
    "name": "Application Layer Protocol"
  },
  {
    "external_id": "T1565",
    "name": "Data Manipulation"
  }
]
```

### CSV:
```csv
Mitigation ID,Mitigation Name,Technique ID,Technique Name
M1037,Filter Network Traffic,T1071,Application Layer Protocol
M1037,Filter Network Traffic,T1565,Data Manipulation
```

### nGQL:
```sql
INSERT VERTEX mitigation(id, name) VALUES `M1037`:("M1037", "Filter Network Traffic");
INSERT VERTEX technique(id, name) VALUES `T1071`:("T1071", "Application Layer Protocol");
INSERT EDGE mitigates() VALUES `M1037` -> `T1071`;
```

## 🏗️ Технические детали

### Архитектура:
- **Язык**: Go 1.25.6 (только стандартная библиотека)
- **Данные**: MITRE ATT&CK STIX 2.0 JSON
- **Кэширование**: Локальная файловая система, Docker volumes, tmpfs
- **Безопасность**: Непривилегированный пользователь, атомарная запись кэша
- **Cloud Native**: 12-Factor App, stateless, конфигурация через env vars

### Особенности:
- **Автоматическое определение контейнера** - разные пути кэша для локальной разработки и Docker
- **Атомарная запись кэша** - использование временных файлов для избежания повреждений
- **Ограничение размера скачиваемых данных** - защита от переполнения памяти
- **Гибкое управление кэшем** - поддержка /dev/null для отключения, tmpfs для памяти

### Docker образ:
- **Размер**: ~13.9MB (чистый образ без кэша)
- **Базовый образ**: Alpine 3.18
- **Пользователь**: appuser:appgroup (UID 1000:GID 1000)
- **Health checks**: Встроенная проверка работоспособности
- **Read-only режим**: Поддерживает запуск с --read-only

## 📁 Структура проекта

```
mitremit-master/
├── mitre-mitigates.go      # Основной исходный код
├── Dockerfile              # Многоступенчатый Dockerfile
├── Makefile                # Управление сборкой и тестированием
├── .dockerignore          # Игнорируемые файлы для Docker
├── .github/workflows/     # GitHub Actions CI/CD
│   └── build.yml          # Workflow для сборки и тестирования
├── .mitre-cache/          # Директория кэша (не в репозитории)
└── README.md              # Эта документация
```

## 🔧 Makefile команды

### Локальная разработка:
- `make build` - сборка бинарника
- `make build-all` - сборка для Linux, macOS, Windows
- `make fmt` - форматирование кода
- `make clean` - очистка артефактов
- `make run` - запуск с примером (M1037)
- `make app-help` - показать справку приложения

### Docker:
- `make docker-build` - сборка Docker образа
- `make docker-run` - запуск с локальным кэшем
- `make docker-run-nocache` - запуск без кэша
- `make docker-run-volume` - запуск с Docker volume
- `make docker-test` - полное тестирование образа
- `make docker-clean` - очистка Docker артефактов

## 🧪 Тестирование

### Docker тесты:
```bash
make docker-test
```

Тесты проверяют:
1. Наличие Docker образа
2. Успешный запуск образа
3. Отсутствие кэша внутри образа
4. Stateless работу с tmpfs
5. Корректность всех форматов вывода

### Пример тестирования в CI/CD:
```bash
# Всегда свежие данные (без кэша)
docker run --rm mitre-sync:latest -mitigation M1037 --no-cache

# Stateless режим (tmpfs)
docker run --rm --tmpfs /tmp:rw,noexec,nosuid,size=100m mitre-sync:latest -mitigation M1037

# Read-only режим (максимальная безопасность)
docker run --rm --read-only --tmpfs /tmp:rw,noexec,nosuid mitre-sync:latest -mitigation M1037 --no-cache
```

## 🔬 Версия Go

Проект использует Go 1.25.6 для обеспечения:
- **Совместимость** с последними улучшениями компилятора
- **Безопасность** - все последние патчи безопасности
- **Производительность** - оптимизации runtime

### Проверка версии Go

```bash
# Локально
go version
# Должно быть: go version go1.25.6 linux/amd64

# В Docker контейнере
docker run --rm mitre-sync:latest /app/mitre-sync -version
```

## 🔒 Безопасность

### Особенности безопасности:
- **Непривилегированный пользователь** - контейнер запускается от appuser (UID 1000)
- **Read-only режим** - поддерживает запуск с флагом --read-only
- **Атомарная запись** - временные файлы для избежания race conditions
- **Ограничение размера** - защита от переполнения памяти при скачивании
- **Stateless** - кэш не сохраняется в образе, только в volumes

### Рекомендации для production:
1. Используйте Docker volumes для сохранения кэша
2. Регулярно обновляйте образ (скачивается свежая версия MITRE данных)
3. Используйте `--no-cache` в CI/CD пайплайнах
4. Рассмотрите запуск в read-only режиме для production

## 📈 Производительность

### Первый запуск:
- Скачивание MITRE бандла (~45MB)
- Парсинг JSON данных
- Кэширование для последующих запусков

### Последующие запуски:
- Использование локального кэша (быстро)
- Поиск в памяти после загрузки данных

### Оптимизации:
- Кэширование в памяти с tmpfs
- Детерминированный вывод для сравнения
- Потоковая обработка больших JSON файлов

## 🤝 Вклад в разработку

### Требования к коду:
1. Соответствие принципам 12-Factor App
2. Поддержка Cloud Native архитектуры
3. Stateless по умолчанию
4. Конфигурация через переменные окружения

### План развития:
- [ ] Модульная архитектура (разделение на пакеты)
- [ ] Unit-тесты и интеграционные тесты
- [ ] Prometheus метрики
- [ ] Helm chart для Kubernetes
- [ ] Полная интеграция с Nebula Graph

## 📄 Лицензия

MIT License

Copyright (c) 2026

Разрешено бесплатное использование, копирование, изменение и распространение при условии указания авторства.

## 🔗 Ссылки

- [MITRE ATT&CK Framework](https://attack.mitre.org/)
- [STIX 2.0 Specification](https://oasis-open.github.io/cti-documentation/stix/intro)
- [Nebula Graph Database](https://nebula-graph.io/)
- [Docker Documentation](https://docs.docker.com/)
- [12-Factor App Methodology](https://12factor.net/)

---

**Примечание**: Данный инструмент предназначен для исследователей безопасности, аналитиков SOC и специалистов по кибербезопасности для анализа взаимосвязей между контрмерами и техниками MITRE ATT&CK.
